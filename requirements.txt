# ============================================================
# Ombre Brain Python 依赖
# 安装: pip install -r requirements.txt
#
# ⚠️ 规矩(2026-09-05 补齐):**每一条都要有上限**。
# 只写 `>=` 等于把「哪天上游发大版本」这件事变成随机定时炸弹 ——
# 2026-07-31 就是这么炸的:`mcp>=1.0.0` 无上限,当天构建拉到刚发布的 2.0.0,
# 而 2.x 移除了 `mcp.server.fastmcp` → OB 连崩三次、502、沈渡记忆工具全断。
# 本地测不出来(本地装的是 1.28.1),只有云端重新构建那一刻才炸。
# 当时只钉了 mcp 一个,其余八个一直裸着;这次全部补上。
#
# 上限取「当前大版本的下一个大版本」,依据是 2026-09-05 从线上容器 pip freeze
# 抄下来的实际版本(与在跑的完全一致):
#   mcp 1.29.1 / rapidfuzz 3.14.6 / openai 3.8.0 / PyYAML 6.0.3 /
#   python-frontmatter 1.3.0 / jieba 0.42.1 / httpx 0.28.1 /
#   numpy 2.5.2 / scikit-learn 1.9.0   (Python 3.12.14)
# httpx 卡到 `<0.29` 而不是 `<1`:它还在 0.x,0.x 的**次版本**就可能破坏兼容。
#
# 上限只在**重新构建**时起作用,不会动到现在跑着的这些版本。
# 要升某个库的大版本,先改代码再放开这里,别只放开这里。
# ============================================================

# MCP 协议 SDK（Claude 通信核心）
# ⚠️ 必须卡 <2:mcp 2.0.0 移除了 `mcp.server.fastmcp`(server.py 就是从这里导入 FastMCP),
# 装上 2.x 会直接 ModuleNotFoundError 起不来。2026-07-31 因 >=1.0.0 无上限拉到刚发布的
# 2.0.0,OB 连崩三次。要升 2.x 得先把 server.py 迁到新 API,不能只放开这里。
mcp>=1.0.0,<2

# 模糊匹配（记忆桶搜索）
rapidfuzz>=3.0.0,<4

# OpenAI 兼容客户端（支持 DeepSeek/Ollama/LM Studio/vLLM/Gemini 等任意兼容 API）
openai>=1.0.0,<4

# YAML 配置解析
pyyaml>=6.0,<7

# Markdown frontmatter 解析（桶文件读写）
python-frontmatter>=1.1.0,<2

# 中文分词
jieba>=0.42.1,<1

# 异步 HTTP 客户端（应用层保活 ping）
httpx>=0.27.0,<0.29

# 向量相似度计算 (导入模式/聚类)
numpy>=1.24.0,<3
scikit-learn>=1.2.0,<2
